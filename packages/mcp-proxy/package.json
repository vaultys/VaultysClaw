{
  "name": "@vaultysclaw/mcp-proxy",
  "version": "0.0.1",
  "description": "MCP front-end for the VaultysClaw proxy — a standalone agent (own VaultysId, own onboarding) exposing the governance-gated reverse-proxy pipeline as MCP tools (stdio or streamable HTTP) for callers that speak MCP instead of raw HTTP",
  "type": "module",
  "bin": {
    "vaultysclaw-mcp-proxy": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "@vaultysclaw/proxy": "workspace:*",
    "@vaultysclaw/sdk": "workspace:*",
    "@vaultysclaw/shared": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3"
  }
}
